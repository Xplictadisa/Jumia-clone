
function Title2({title}: {title: string}) {
  return <div className="h-12 rounded-t-[5px] flex items-center px-6">
          <span className="font-semibold text-[22px]">{title}</span>
        </div>
}

export default Title2